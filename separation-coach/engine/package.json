{
  "name": "@rosie-bear/separation-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic progression engine for the Rosie Bear Separation Coach. Framework-agnostic, pure, unit-tested. Given (history, profile) it returns today's target. It never lets a model choose a duration.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "sim": "tsx sim/simulate.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
